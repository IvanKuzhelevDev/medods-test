# МИС Task Tracker API — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Ruby on Rails 8 API-only service for a medical task tracker with full CRUD, many-to-many tags (3 protected system tags), and calendar-style recurring tasks whose per-day instances carry independent state and one-off exceptions.

**Architecture:** A `Task` is a *series/definition* holding a recurrence rule. Concrete per-day occurrences are **computed on the fly** for a requested date window (`Recurrence::Generator`) and only **materialized** into `task_occurrences` rows when an instance diverges from the rule (completed, rescheduled, cancelled, or edited). The calendar endpoint (`Occurrences::Calendar`) expands rules across a bounded window and overlays materialized exceptions. This solves the "infinity problem", independent per-instance status, and one-off exceptions without storing millions of rows.

**Tech Stack:** Ruby 3.4, Rails 8 (API), PostgreSQL 16, RSpec + FactoryBot, rswag (Swagger/OpenAPI), Pagy, Docker Compose. All product docs in Russian; code/identifiers in English.

**Key decisions (confirmed with stakeholder):**
- Recurrence = rule + virtual occurrences + materialized exceptions.
- No authentication / no users (kept strictly within the task scope).
- Fully Dockerized (Ruby 3.4 + PG 16 in containers).
- Documentation in Russian, code in English.

**Conventions:**
- Statuses are stored as a plain string column with inclusion validation (NOT Rails `enum`, to avoid the `new` label colliding with `ActiveRecord#new`). Values: `new`, `in_progress`, `done`, `canceled`.
- Recurrence types: `once`, `daily`, `monthly`, `specific_dates`, `parity`.
- Dates are calendar `Date`s in a single timezone (UTC by default); `due_time` stores the time-of-day. This is documented as an explicit assumption.
- All commands run **inside Docker**. Prefix shell commands with `docker compose run --rm web ...` unless noted. The stack uses one web service and one `db` (postgres:16) service.

---

## File Structure

```
Dockerfile                         # dev image (ruby:3.4-slim)
docker-compose.yml                 # web + db(postgres:16)
.dockerignore
Gemfile / Gemfile.lock
config/database.yml                # ENV-driven, points at db service
config/routes.rb                   # /api/v1 namespace
config/initializers/pagy.rb

app/models/task.rb                 # series/definition + recurrence param validation
app/models/task_occurrence.rb      # materialized exception/instance
app/models/tag.rb                  # system-tag protection
app/models/task_tag.rb             # join

app/services/recurrence/generator.rb       # rule -> dates in window  (THE engine)
app/services/occurrences/calendar.rb        # window -> occurrence views (virtual + overrides)
app/services/occurrences/view.rb            # value object for one occurrence

app/serializers/task_serializer.rb
app/serializers/tag_serializer.rb
app/serializers/occurrence_serializer.rb

app/controllers/api/v1/base_controller.rb       # error handling, pagination helpers
app/controllers/api/v1/tasks_controller.rb
app/controllers/api/v1/tags_controller.rb
app/controllers/api/v1/task_tags_controller.rb  # attach/detach tag <-> task
app/controllers/api/v1/occurrences_controller.rb

db/seeds.rb                        # idempotent system tags

spec/...                           # model, service, request specs
swagger/v1/swagger.yaml            # generated by rswag
README.md                          # Russian: run instructions, assumptions, API overview
HISTORY.md                         # curated LLM chat history (done separately at the end)
```

Each service object has one responsibility and is independently unit-tested. Controllers stay thin and delegate to services/serializers.

---

## Task 0: Dockerized project bootstrap

Generates the Rails app entirely inside containers — no local Ruby 3.4 needed.

**Files:**
- Create: `Dockerfile`, `Gemfile`, `Gemfile.lock` (empty), `docker-compose.yml`, `.dockerignore`
- Generated by Rails: the whole app skeleton
- Modify after generation: `config/database.yml`

- [ ] **Step 1: Install Docker Desktop (manual, host machine)**

```bash
brew install --cask docker
open -a Docker      # launch and wait until it's running
docker info         # must succeed before continuing
```

- [ ] **Step 2: Create the bootstrap `Dockerfile`**

```dockerfile
FROM ruby:3.4-slim

RUN apt-get update -qq \
  && apt-get install -y --no-install-recommends build-essential libpq-dev libyaml-dev pkg-config git curl \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY Gemfile Gemfile.lock ./
RUN bundle install

COPY . .

EXPOSE 3000
CMD ["bin/rails", "server", "-b", "0.0.0.0"]
```

- [ ] **Step 3: Create a minimal `Gemfile` + empty `Gemfile.lock`**

`Gemfile`:
```ruby
source "https://rubygems.org"
gem "rails", "~> 8.0"
```

`Gemfile.lock`: empty file (`> Gemfile.lock`).

- [ ] **Step 4: Create `.dockerignore`**

```
.git
log/*
tmp/*
storage/*
node_modules
.DS_Store
```

- [ ] **Step 5: Create `docker-compose.yml`**

```yaml
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 5

  web:
    build: .
    command: bash -c "bin/rails db:prepare && bin/rails server -b 0.0.0.0 -p 3000"
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    environment:
      DATABASE_HOST: db
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres
      RAILS_ENV: development
    depends_on:
      db:
        condition: service_healthy

volumes:
  pg_data:
```

- [ ] **Step 6: Build the bootstrap image**

Run: `docker compose build`
Expected: image builds, `bundle install` installs Rails 8.

- [ ] **Step 7: Generate the Rails app into the current directory**

Run:
```bash
docker compose run --rm --no-deps web \
  rails new . --api --database=postgresql --skip-test --skip-docker --force
```
Notes: `--skip-test` (we use RSpec), `--skip-docker` (we own the Docker setup so Rails won't generate a conflicting production Dockerfile), `--force` overwrites the placeholder `Gemfile`. Pre-existing `docs/`, plan files, `Dockerfile`, `docker-compose.yml` are preserved.
Expected: full Rails skeleton appears on the host via the bind mount.

- [ ] **Step 8: Rebuild the image with the real Gemfile**

Run: `docker compose build`
Expected: success (Rails default gems installed).

- [ ] **Step 9: Configure `config/database.yml`** (replace file contents)

```yaml
default: &default
  adapter: postgresql
  encoding: unicode
  host: <%= ENV.fetch("DATABASE_HOST", "localhost") %>
  username: <%= ENV.fetch("DATABASE_USER", "postgres") %>
  password: <%= ENV.fetch("DATABASE_PASSWORD", "postgres") %>
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>

development:
  <<: *default
  database: med_task_tracker_development

test:
  <<: *default
  database: med_task_tracker_test

production:
  <<: *default
  database: med_task_tracker_production
```

- [ ] **Step 10: Prepare DB and verify the app boots**

Run:
```bash
docker compose run --rm web bin/rails db:prepare
docker compose up -d
curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:3000/up
```
Expected: `200` (Rails health check). Then `docker compose down`.

- [ ] **Step 11: Commit**

```bash
git add -A
git commit -m "chore: bootstrap dockerized Rails 8 API skeleton"
```

---

## Task 1: RSpec, FactoryBot, Pagy & API base controller

**Files:**
- Modify: `Gemfile`
- Create: `spec/spec_helper.rb`, `spec/rails_helper.rb` (via generator), `spec/support/factory_bot.rb`, `config/initializers/pagy.rb`
- Create: `app/controllers/api/v1/base_controller.rb`
- Test: `spec/requests/health_spec.rb`

- [ ] **Step 1: Add gems to `Gemfile`**

In the `:development, :test` group add:
```ruby
gem "rspec-rails", "~> 7.1"
gem "factory_bot_rails"
```
At top level (all environments) add:
```ruby
gem "pagy", "~> 9.0"
```

- [ ] **Step 2: Bundle and install RSpec**

```bash
docker compose build
docker compose run --rm web bin/rails generate rspec:install
```

- [ ] **Step 3: Configure FactoryBot**

Create `spec/support/factory_bot.rb`:
```ruby
RSpec.configure do |config|
  config.include FactoryBot::Syntax::Methods
end
```
In `spec/rails_helper.rb`, uncomment the support-files loader line:
```ruby
Dir[Rails.root.join("spec/support/**/*.rb")].each { |f| require f }
```

- [ ] **Step 4: Configure Pagy**

Create `config/initializers/pagy.rb`:
```ruby
require "pagy/extras/array"
Pagy::DEFAULT[:limit] = 25
Pagy::DEFAULT[:max_limit] = 100
```

- [ ] **Step 5: Write the failing smoke test**

Create `spec/requests/health_spec.rb`:
```ruby
require "rails_helper"

RSpec.describe "Health", type: :request do
  it "returns 200 from the Rails health endpoint" do
    get "/up"
    expect(response).to have_http_status(:ok)
  end
end
```

- [ ] **Step 6: Run it**

Run: `docker compose run --rm web bundle exec rspec spec/requests/health_spec.rb`
Expected: PASS.

- [ ] **Step 7: Create the API base controller** (error envelope + pagination)

Create `app/controllers/api/v1/base_controller.rb`:
```ruby
module Api
  module V1
    class BaseController < ActionController::API
      include Pagy::Backend

      rescue_from ActiveRecord::RecordNotFound, with: :render_not_found
      rescue_from ActiveRecord::RecordInvalid, with: :render_unprocessable
      rescue_from ActionController::ParameterMissing, with: :render_bad_request

      private

      def render_not_found(error)
        render json: { error: { message: "Запись не найдена", details: error.message } },
               status: :not_found
      end

      def render_unprocessable(error)
        render json: { error: { message: "Ошибка валидации", details: error.record.errors.full_messages } },
               status: :unprocessable_entity
      end

      def render_bad_request(error)
        render json: { error: { message: "Некорректный запрос", details: error.message } },
               status: :bad_request
      end

      def pagination_meta(pagy)
        { page: pagy.page, items: pagy.limit, count: pagy.count, pages: pagy.pages }
      end
    end
  end
end
```

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "chore: add rspec, factory_bot, pagy, API base controller"
```

---

## Task 2: Tag model + system-tag protection (TDD)

**Files:**
- Create: migration `db/migrate/*_create_tags.rb`
- Create: `app/models/tag.rb`
- Create: `db/seeds.rb` (system tags)
- Test: `spec/models/tag_spec.rb`, `spec/factories/tags.rb`

- [ ] **Step 1: Create the migration**

```bash
docker compose run --rm web bin/rails g migration CreateTags
```
Edit it to:
```ruby
class CreateTags < ActiveRecord::Migration[8.0]
  def change
    create_table :tags do |t|
      t.string :name, null: false
      t.boolean :system, null: false, default: false
      t.timestamps
    end
    add_index :tags, "lower(name)", unique: true, name: "index_tags_on_lower_name"
  end
end
```

- [ ] **Step 2: Create the factory**

`spec/factories/tags.rb`:
```ruby
FactoryBot.define do
  factory :tag do
    sequence(:name) { |n| "tag-#{n}" }
    system { false }

    trait :system_tag do
      system { true }
    end
  end
end
```

- [ ] **Step 3: Write failing model specs**

`spec/models/tag_spec.rb`:
```ruby
require "rails_helper"

RSpec.describe Tag, type: :model do
  it "requires a name" do
    expect(build(:tag, name: nil)).not_to be_valid
  end

  it "enforces case-insensitive uniqueness" do
    create(:tag, name: "Операции")
    expect(build(:tag, name: "операции")).not_to be_valid
  end

  it "allows creating and editing a custom tag" do
    tag = create(:tag, name: "обход")
    expect(tag.update(name: "обходы")).to be(true)
  end

  it "forbids renaming a system tag" do
    tag = create(:tag, :system_tag, name: "операции")
    expect(tag.update(name: "хирургия")).to be(false)
    expect(tag.errors[:base]).to be_present
  end

  it "forbids destroying a system tag" do
    tag = create(:tag, :system_tag, name: "звонок")
    expect(tag.destroy).to be(false)
    expect(Tag.exists?(tag.id)).to be(true)
  end
end
```

- [ ] **Step 4: Run, expect failure**

Run: `docker compose run --rm web bundle exec rspec spec/models/tag_spec.rb`
Expected: FAIL (Tag undefined / migration not run).

- [ ] **Step 5: Migrate and implement the model**

```bash
docker compose run --rm web bin/rails db:migrate
```
`app/models/tag.rb`:
```ruby
class Tag < ApplicationRecord
  SYSTEM_NAMES = %w[отчётность операции звонок].freeze

  has_many :task_tags, dependent: :destroy
  has_many :tasks, through: :task_tags

  validates :name, presence: true,
                   uniqueness: { case_sensitive: false }

  before_update :prevent_system_modification
  before_destroy :prevent_system_destroy

  scope :system_tags, -> { where(system: true) }

  private

  def prevent_system_modification
    return unless system?
    return unless will_save_change_to_name? || will_save_change_to_system?

    errors.add(:base, "Системные теги нельзя изменять")
    throw :abort
  end

  def prevent_system_destroy
    return unless system?

    errors.add(:base, "Системные теги нельзя удалять")
    throw :abort
  end
end
```

- [ ] **Step 6: Run, expect pass**

Run: `docker compose run --rm web bundle exec rspec spec/models/tag_spec.rb`
Expected: PASS.

- [ ] **Step 7: Idempotent seeds for the 3 system tags**

`db/seeds.rb`:
```ruby
Tag::SYSTEM_NAMES.each do |name|
  Tag.find_or_create_by!(name: name) { |t| t.system = true }
end
```
Run and verify:
```bash
docker compose run --rm web bin/rails db:seed
docker compose run --rm web bin/rails runner 'puts Tag.system_tags.pluck(:name).inspect'
```
Expected: `["отчётность", "операции", "звонок"]`.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat: tag model with protected system tags + seeds"
```

---

## Task 3: Task model + recurrence-parameter validation (TDD)

**Files:**
- Create: migration `*_create_tasks.rb`
- Create: `app/models/task.rb`
- Test: `spec/models/task_spec.rb`, `spec/factories/tasks.rb`

- [ ] **Step 1: Create the migration**

```bash
docker compose run --rm web bin/rails g migration CreateTasks
```
Edit:
```ruby
class CreateTasks < ActiveRecord::Migration[8.0]
  def change
    create_table :tasks do |t|
      t.string  :title, null: false
      t.text    :description
      t.string  :status, null: false, default: "new"

      t.string  :recurrence_type, null: false, default: "once"
      t.integer :recurrence_interval
      t.integer :days_of_month, array: true, default: []
      t.date    :specific_dates, array: true, default: []
      t.string  :parity
      t.date    :starts_on
      t.date    :ends_on
      t.time    :due_time

      t.timestamps
    end
    add_index :tasks, :status
    add_index :tasks, :starts_on
  end
end
```

- [ ] **Step 2: Create the factory (with traits per recurrence type)**

`spec/factories/tasks.rb`:
```ruby
FactoryBot.define do
  factory :task do
    sequence(:title) { |n| "Задача #{n}" }
    description { "Описание" }
    status { "new" }
    recurrence_type { "once" }
    starts_on { Date.new(2026, 6, 1) }

    trait :daily do
      recurrence_type { "daily" }
      recurrence_interval { 1 }
    end

    trait :monthly do
      recurrence_type { "monthly" }
      days_of_month { [1, 15] }
    end

    trait :specific do
      recurrence_type { "specific_dates" }
      specific_dates { [Date.new(2026, 6, 3), Date.new(2026, 6, 10)] }
      starts_on { nil }
    end

    trait :parity do
      recurrence_type { "parity" }
      parity { "even" }
    end
  end
end
```

- [ ] **Step 3: Write failing model specs**

`spec/models/task_spec.rb`:
```ruby
require "rails_helper"

RSpec.describe Task, type: :model do
  it "requires a title" do
    expect(build(:task, title: nil)).not_to be_valid
  end

  it "validates status inclusion" do
    expect(build(:task, status: "bogus")).not_to be_valid
  end

  it "validates recurrence_type inclusion" do
    expect(build(:task, recurrence_type: "weekly")).not_to be_valid
  end

  describe "recurrence parameter validation" do
    it "requires a positive interval for daily" do
      expect(build(:task, :daily, recurrence_interval: 0)).not_to be_valid
    end

    it "requires days_of_month within 1..31 for monthly" do
      expect(build(:task, :monthly, days_of_month: [0, 32])).not_to be_valid
      expect(build(:task, :monthly, days_of_month: [1, 31])).to be_valid
    end

    it "requires non-empty specific_dates" do
      expect(build(:task, :specific, specific_dates: [])).not_to be_valid
    end

    it "requires parity in even/odd" do
      expect(build(:task, :parity, parity: "weird")).not_to be_valid
    end

    it "requires ends_on to be on or after starts_on" do
      expect(build(:task, :daily, starts_on: Date.new(2026, 6, 10), ends_on: Date.new(2026, 6, 1)))
        .not_to be_valid
    end
  end
end
```

- [ ] **Step 4: Run, expect failure**

Run: `docker compose run --rm web bundle exec rspec spec/models/task_spec.rb`
Expected: FAIL.

- [ ] **Step 5: Migrate and implement the model**

```bash
docker compose run --rm web bin/rails db:migrate
```
`app/models/task.rb`:
```ruby
class Task < ApplicationRecord
  STATUSES         = %w[new in_progress done canceled].freeze
  RECURRENCE_TYPES = %w[once daily monthly specific_dates parity].freeze
  PARITIES         = %w[even odd].freeze

  has_many :task_tags, dependent: :destroy
  has_many :tags, through: :task_tags
  has_many :occurrences, class_name: "TaskOccurrence", dependent: :destroy

  validates :title, presence: true
  validates :status, inclusion: { in: STATUSES }
  validates :recurrence_type, inclusion: { in: RECURRENCE_TYPES }
  validate  :recurrence_params_valid
  validate  :ends_on_after_starts_on

  scope :with_status, ->(s) { where(status: s) if s.present? }

  private

  def recurrence_params_valid
    case recurrence_type
    when "once"
      errors.add(:starts_on, "обязателен для разовой задачи") if starts_on.blank?
    when "daily"
      errors.add(:starts_on, "обязателен") if starts_on.blank?
      if recurrence_interval.blank? || recurrence_interval < 1
        errors.add(:recurrence_interval, "должен быть >= 1")
      end
    when "monthly"
      errors.add(:starts_on, "обязателен") if starts_on.blank?
      if days_of_month.blank? || days_of_month.any? { |d| !d.between?(1, 31) }
        errors.add(:days_of_month, "должны быть числами от 1 до 31")
      end
    when "specific_dates"
      errors.add(:specific_dates, "не должны быть пустыми") if specific_dates.blank?
    when "parity"
      errors.add(:starts_on, "обязателен") if starts_on.blank?
      errors.add(:parity, "должна быть even или odd") unless PARITIES.include?(parity)
    end
  end

  def ends_on_after_starts_on
    return if starts_on.blank? || ends_on.blank?

    errors.add(:ends_on, "не может быть раньше даты начала") if ends_on < starts_on
  end
end
```

- [ ] **Step 6: Run, expect pass**

Run: `docker compose run --rm web bundle exec rspec spec/models/task_spec.rb`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: task model with recurrence parameter validation"
```

---

## Task 4: TaskTag join model (TDD)

**Files:**
- Create: migration `*_create_task_tags.rb`
- Create: `app/models/task_tag.rb`
- Test: `spec/models/task_tag_spec.rb`

- [ ] **Step 1: Migration**

```bash
docker compose run --rm web bin/rails g migration CreateTaskTags
```
Edit:
```ruby
class CreateTaskTags < ActiveRecord::Migration[8.0]
  def change
    create_table :task_tags do |t|
      t.references :task, null: false, foreign_key: true
      t.references :tag, null: false, foreign_key: true
      t.timestamps
    end
    add_index :task_tags, [:task_id, :tag_id], unique: true
  end
end
```

- [ ] **Step 2: Failing spec**

`spec/models/task_tag_spec.rb`:
```ruby
require "rails_helper"

RSpec.describe "Task <-> Tag association", type: :model do
  it "attaches tags to a task many-to-many" do
    task = create(:task)
    tag  = create(:tag, name: "обход")
    task.tags << tag
    expect(task.reload.tags).to include(tag)
    expect(tag.reload.tasks).to include(task)
  end

  it "prevents duplicate task/tag pairs" do
    task = create(:task)
    tag  = create(:tag, name: "обход")
    task.tags << tag
    expect { task.task_tags.create!(tag: tag) }
      .to raise_error(ActiveRecord::RecordNotUnique)
  end
end
```

- [ ] **Step 3: Run, expect failure**

Run: `docker compose run --rm web bundle exec rspec spec/models/task_tag_spec.rb`
Expected: FAIL.

- [ ] **Step 4: Migrate and implement**

```bash
docker compose run --rm web bin/rails db:migrate
```
`app/models/task_tag.rb`:
```ruby
class TaskTag < ApplicationRecord
  belongs_to :task
  belongs_to :tag

  validates :tag_id, uniqueness: { scope: :task_id }
end
```

- [ ] **Step 5: Run, expect pass**

Run: `docker compose run --rm web bundle exec rspec spec/models/task_tag_spec.rb`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: task_tags join model (many-to-many)"
```

---

## Task 5: TaskOccurrence model (TDD)

The materialized exception/instance. Validates that an occurrence_date is a real slot of its series (reuses the generator from Task 6 — so this task's "slot validity" spec is added in Task 6 once the generator exists; here we cover the structural validations).

**Files:**
- Create: migration `*_create_task_occurrences.rb`
- Create: `app/models/task_occurrence.rb`
- Test: `spec/models/task_occurrence_spec.rb`, `spec/factories/task_occurrences.rb`

- [ ] **Step 1: Migration**

```bash
docker compose run --rm web bin/rails g migration CreateTaskOccurrences
```
Edit:
```ruby
class CreateTaskOccurrences < ActiveRecord::Migration[8.0]
  def change
    create_table :task_occurrences do |t|
      t.references :task, null: false, foreign_key: true
      t.date     :occurrence_date, null: false
      t.string   :status
      t.datetime :scheduled_at
      t.string   :title
      t.text     :description
      t.boolean  :canceled, null: false, default: false
      t.timestamps
    end
    add_index :task_occurrences, [:task_id, :occurrence_date], unique: true
  end
end
```

- [ ] **Step 2: Factory**

`spec/factories/task_occurrences.rb`:
```ruby
FactoryBot.define do
  factory :task_occurrence do
    association :task, :daily
    occurrence_date { Date.new(2026, 6, 1) }
    status { "done" }
  end
end
```

- [ ] **Step 3: Failing spec (structural validations)**

`spec/models/task_occurrence_spec.rb`:
```ruby
require "rails_helper"

RSpec.describe TaskOccurrence, type: :model do
  it "requires an occurrence_date" do
    expect(build(:task_occurrence, occurrence_date: nil)).not_to be_valid
  end

  it "is unique per (task, date)" do
    occ = create(:task_occurrence)
    dup = build(:task_occurrence, task: occ.task, occurrence_date: occ.occurrence_date)
    expect(dup).not_to be_valid
  end

  it "validates status inclusion when present" do
    expect(build(:task_occurrence, status: "bogus")).not_to be_valid
  end

  it "allows a nil status (falls back to series default)" do
    expect(build(:task_occurrence, status: nil)).to be_valid
  end
end
```

- [ ] **Step 4: Run, expect failure**

Run: `docker compose run --rm web bundle exec rspec spec/models/task_occurrence_spec.rb`
Expected: FAIL.

- [ ] **Step 5: Migrate and implement (slot validation reuses generator)**

```bash
docker compose run --rm web bin/rails db:migrate
```
`app/models/task_occurrence.rb`:
```ruby
class TaskOccurrence < ApplicationRecord
  belongs_to :task

  validates :occurrence_date, presence: true,
                              uniqueness: { scope: :task_id }
  validates :status, inclusion: { in: Task::STATUSES }, allow_nil: true
  validate  :date_is_a_real_slot

  private

  # An occurrence may only be materialized for a date the series actually
  # schedules. Reschedules change scheduled_at (time), never occurrence_date.
  def date_is_a_real_slot
    return if occurrence_date.blank? || task.blank?

    scheduled = Recurrence::Generator.new(task).dates_between(occurrence_date, occurrence_date)
    return if scheduled.include?(occurrence_date)

    errors.add(:occurrence_date, "не входит в расписание задачи")
  end
end
```

- [ ] **Step 6: Run, expect pass** (the daily factory's date 2026-06-01 == starts_on, a valid slot)

Run: `docker compose run --rm web bundle exec rspec spec/models/task_occurrence_spec.rb`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: task_occurrence model with slot validation"
```

---

## Task 6: Recurrence::Generator — the engine (TDD)

The crown jewel. Pure, dependency-free date math. Thoroughly tested.

**Files:**
- Create: `app/services/recurrence/generator.rb`
- Test: `spec/services/recurrence/generator_spec.rb`

- [ ] **Step 1: Write comprehensive failing specs**

`spec/services/recurrence/generator_spec.rb`:
```ruby
require "rails_helper"

RSpec.describe Recurrence::Generator do
  def dates(task, from, to)
    described_class.new(task).dates_between(from, to)
  end

  describe "once" do
    it "returns the single date if inside the window" do
      task = build(:task, recurrence_type: "once", starts_on: Date.new(2026, 6, 10))
      expect(dates(task, Date.new(2026, 6, 1), Date.new(2026, 6, 30)))
        .to eq([Date.new(2026, 6, 10)])
    end

    it "returns nothing if outside the window" do
      task = build(:task, recurrence_type: "once", starts_on: Date.new(2026, 5, 10))
      expect(dates(task, Date.new(2026, 6, 1), Date.new(2026, 6, 30))).to eq([])
    end
  end

  describe "daily" do
    it "returns every day when interval is 1" do
      task = build(:task, :daily, recurrence_interval: 1, starts_on: Date.new(2026, 6, 1))
      result = dates(task, Date.new(2026, 6, 1), Date.new(2026, 6, 5))
      expect(result).to eq((1..5).map { |d| Date.new(2026, 6, d) })
    end

    it "respects an interval and aligns to starts_on" do
      task = build(:task, :daily, recurrence_interval: 3, starts_on: Date.new(2026, 6, 1))
      result = dates(task, Date.new(2026, 6, 2), Date.new(2026, 6, 12))
      expect(result).to eq([Date.new(2026, 6, 4), Date.new(2026, 6, 7), Date.new(2026, 6, 10)])
    end

    it "never exceeds ends_on" do
      task = build(:task, :daily, recurrence_interval: 1,
                   starts_on: Date.new(2026, 6, 1), ends_on: Date.new(2026, 6, 3))
      result = dates(task, Date.new(2026, 6, 1), Date.new(2026, 6, 30))
      expect(result).to eq([Date.new(2026, 6, 1), Date.new(2026, 6, 2), Date.new(2026, 6, 3)])
    end

    it "stays bounded for an open-ended series (infinity problem)" do
      task = build(:task, :daily, recurrence_interval: 1, starts_on: Date.new(2020, 1, 1), ends_on: nil)
      result = dates(task, Date.new(2026, 6, 1), Date.new(2026, 6, 7))
      expect(result.size).to eq(7)
    end
  end

  describe "monthly" do
    it "returns the listed days each month" do
      task = build(:task, :monthly, days_of_month: [1, 15], starts_on: Date.new(2026, 6, 1))
      result = dates(task, Date.new(2026, 6, 1), Date.new(2026, 7, 31))
      expect(result).to eq([
        Date.new(2026, 6, 1), Date.new(2026, 6, 15),
        Date.new(2026, 7, 1), Date.new(2026, 7, 15)
      ])
    end

    it "skips invalid days instead of rolling over (Feb 31)" do
      task = build(:task, :monthly, days_of_month: [31], starts_on: Date.new(2026, 1, 1))
      result = dates(task, Date.new(2026, 1, 1), Date.new(2026, 3, 31))
      expect(result).to eq([Date.new(2026, 1, 31), Date.new(2026, 3, 31)]) # no Feb
    end
  end

  describe "specific_dates" do
    it "returns only the listed dates within the window" do
      task = build(:task, :specific,
                   specific_dates: [Date.new(2026, 6, 3), Date.new(2026, 6, 20), Date.new(2026, 7, 1)])
      result = dates(task, Date.new(2026, 6, 1), Date.new(2026, 6, 30))
      expect(result).to eq([Date.new(2026, 6, 3), Date.new(2026, 6, 20)])
    end
  end

  describe "parity" do
    it "returns only even days when parity is even" do
      task = build(:task, :parity, parity: "even", starts_on: Date.new(2026, 6, 1))
      result = dates(task, Date.new(2026, 6, 1), Date.new(2026, 6, 6))
      expect(result).to eq([Date.new(2026, 6, 2), Date.new(2026, 6, 4), Date.new(2026, 6, 6)])
    end

    it "returns only odd days when parity is odd" do
      task = build(:task, :parity, parity: "odd", starts_on: Date.new(2026, 6, 1))
      result = dates(task, Date.new(2026, 6, 1), Date.new(2026, 6, 5))
      expect(result).to eq([Date.new(2026, 6, 1), Date.new(2026, 6, 3), Date.new(2026, 6, 5)])
    end
  end

  describe "window cap" do
    it "clamps an oversized window to MAX_WINDOW_DAYS" do
      task = build(:task, :daily, recurrence_interval: 1, starts_on: Date.new(2000, 1, 1))
      result = dates(task, Date.new(2026, 1, 1), Date.new(2099, 1, 1))
      expect(result.size).to eq(Recurrence::Generator::MAX_WINDOW_DAYS + 1)
    end
  end
end
```

- [ ] **Step 2: Run, expect failure**

Run: `docker compose run --rm web bundle exec rspec spec/services/recurrence/generator_spec.rb`
Expected: FAIL (`uninitialized constant Recurrence::Generator`).

- [ ] **Step 3: Implement the generator**

`app/services/recurrence/generator.rb`:
```ruby
module Recurrence
  class Generator
    MAX_WINDOW_DAYS = 366

    def initialize(task)
      @task = task
    end

    # Returns a sorted Array<Date> of scheduled dates within [from, to].
    def dates_between(from, to)
      to = [to, from + MAX_WINDOW_DAYS].min
      return [] if from > to

      case @task.recurrence_type
      when "once"           then once_dates(from, to)
      when "daily"          then daily_dates(from, to)
      when "monthly"        then monthly_dates(from, to)
      when "specific_dates" then specific_dates(from, to)
      when "parity"         then parity_dates(from, to)
      else []
      end
    end

    private

    # Intersect the query window with the series' own [starts_on, ends_on].
    def effective_range(from, to)
      lower = [@task.starts_on, from].compact.max
      upper = @task.ends_on ? [@task.ends_on, to].min : to
      [lower, upper]
    end

    def once_dates(from, to)
      d = @task.starts_on
      d && d.between?(from, to) ? [d] : []
    end

    def daily_dates(from, to)
      lower, upper = effective_range(from, to)
      return [] if lower.nil? || lower > upper

      step = @task.recurrence_interval || 1
      offset = (lower - @task.starts_on).to_i
      remainder = offset % step
      first = remainder.zero? ? lower : lower + (step - remainder)
      (first..upper).step(step).to_a
    end

    def monthly_dates(from, to)
      lower, upper = effective_range(from, to)
      return [] if lower.nil? || lower > upper

      days = Array(@task.days_of_month)
      result = []
      each_month(lower, upper) do |year, month|
        days.each do |day|
          next unless Date.valid_date?(year, month, day)

          d = Date.new(year, month, day)
          result << d if d.between?(lower, upper)
        end
      end
      result.sort
    end

    def specific_dates(from, to)
      lower, upper = effective_range(from, to)
      Array(@task.specific_dates).select { |d| d.between?(lower, upper) }.sort
    end

    def parity_dates(from, to)
      lower, upper = effective_range(from, to)
      return [] if lower.nil? || lower > upper

      want_even = @task.parity == "even"
      (lower..upper).select { |d| d.day.even? == want_even }
    end

    def each_month(from, to)
      cursor = from.beginning_of_month
      while cursor <= to
        yield cursor.year, cursor.month
        cursor = cursor.next_month
      end
    end
  end
end
```

- [ ] **Step 4: Run, expect pass**

Run: `docker compose run --rm web bundle exec rspec spec/services/recurrence/generator_spec.rb`
Expected: PASS (all examples).

- [ ] **Step 5: Add the slot-validity spec for TaskOccurrence (now that the generator exists)**

Append to `spec/models/task_occurrence_spec.rb`:
```ruby
  it "rejects a date that is not part of the series schedule" do
    task = create(:task, :monthly, days_of_month: [1], starts_on: Date.new(2026, 6, 1))
    occ  = build(:task_occurrence, task: task, occurrence_date: Date.new(2026, 6, 2))
    expect(occ).not_to be_valid
    expect(occ.errors[:occurrence_date]).to be_present
  end
```

- [ ] **Step 6: Run, expect pass**

Run: `docker compose run --rm web bundle exec rspec spec/models/task_occurrence_spec.rb`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: recurrence generator engine with full coverage"
```

---

## Task 7: Occurrences::Calendar projection (TDD)

Expands rules across a window and overlays materialized exceptions into occurrence views.

**Files:**
- Create: `app/services/occurrences/view.rb`
- Create: `app/services/occurrences/calendar.rb`
- Test: `spec/services/occurrences/calendar_spec.rb`

- [ ] **Step 1: Write failing specs**

`spec/services/occurrences/calendar_spec.rb`:
```ruby
require "rails_helper"

RSpec.describe Occurrences::Calendar do
  let(:from) { Date.new(2026, 6, 1) }
  let(:to)   { Date.new(2026, 6, 7) }

  def views(**opts)
    described_class.new(scope: Task.all, from: from, to: to, **opts).occurrences
  end

  it "expands a daily series into one view per day" do
    create(:task, :daily, recurrence_interval: 1, starts_on: from, title: "Обход")
    result = views
    expect(result.size).to eq(7)
    expect(result.map(&:date)).to eq((1..7).map { |d| Date.new(2026, 6, d) })
    expect(result).to all(have_attributes(status: "new", exception: false))
  end

  it "overlays a materialized occurrence's independent status" do
    task = create(:task, :daily, recurrence_interval: 1, starts_on: from)
    create(:task_occurrence, task: task, occurrence_date: Date.new(2026, 6, 3), status: "done")

    result = views
    done = result.find { |v| v.date == Date.new(2026, 6, 3) }
    other = result.find { |v| v.date == Date.new(2026, 6, 4) }

    expect(done.status).to eq("done")
    expect(done.exception).to be(true)
    expect(other.status).to eq("new") # neighbours unaffected
  end

  it "reflects a rescheduled time on the exception view" do
    task = create(:task, :daily, recurrence_interval: 1, starts_on: from)
    moved_at = Time.utc(2026, 6, 3, 14, 0)
    create(:task_occurrence, task: task, occurrence_date: Date.new(2026, 6, 3), scheduled_at: moved_at)

    view = views.find { |v| v.date == Date.new(2026, 6, 3) }
    expect(view.scheduled_at).to eq(moved_at)
  end

  it "marks a canceled occurrence" do
    task = create(:task, :daily, recurrence_interval: 1, starts_on: from)
    create(:task_occurrence, task: task, occurrence_date: Date.new(2026, 6, 3),
                             canceled: true, status: "canceled")

    view = views.find { |v| v.date == Date.new(2026, 6, 3) }
    expect(view.canceled).to be(true)
  end

  it "filters by effective status" do
    task = create(:task, :daily, recurrence_interval: 1, starts_on: from)
    create(:task_occurrence, task: task, occurrence_date: Date.new(2026, 6, 3), status: "done")

    expect(views(status: "done").map(&:date)).to eq([Date.new(2026, 6, 3)])
  end

  it "filters by tag" do
    tagged = create(:task, :daily, recurrence_interval: 1, starts_on: from)
    create(:task, :daily, recurrence_interval: 1, starts_on: from) # untagged
    tag = create(:tag, name: "обход")
    tagged.tags << tag

    result = views(tag: "обход")
    expect(result.map(&:task).uniq).to eq([tagged])
  end
end
```

- [ ] **Step 2: Run, expect failure**

Run: `docker compose run --rm web bundle exec rspec spec/services/occurrences/calendar_spec.rb`
Expected: FAIL.

- [ ] **Step 3: Implement the view value object**

`app/services/occurrences/view.rb`:
```ruby
module Occurrences
  # A single calendar occurrence — either virtual (computed) or backed by a
  # materialized TaskOccurrence exception.
  View = Struct.new(
    :task, :date, :status, :scheduled_at,
    :title, :description, :canceled, :exception, :tags,
    keyword_init: true
  )
end
```

- [ ] **Step 4: Implement the calendar projection**

`app/services/occurrences/calendar.rb`:
```ruby
module Occurrences
  class Calendar
    def initialize(scope: Task.all, from:, to:, status: nil, tag: nil)
      @scope  = scope
      @from   = from
      @to     = to
      @status = status.presence
      @tag    = tag.presence
    end

    def occurrences
      tasks = @scope.includes(:tags, :occurrences)
      tasks = tasks.joins(:tags).where(tags: { name: @tag }).distinct if @tag

      views = tasks.flat_map { |task| views_for(task) }
      views = views.select { |v| v.status == @status } if @status
      views.sort_by { |v| [v.scheduled_at || v.date.to_time, v.task.id] }
    end

    private

    def views_for(task)
      overrides = task.occurrences.index_by(&:occurrence_date)
      Recurrence::Generator.new(task).dates_between(@from, @to).map do |date|
        build_view(task, date, overrides[date])
      end
    end

    def build_view(task, date, override)
      View.new(
        task: task,
        date: date,
        status: override&.status || task.status,
        scheduled_at: override&.scheduled_at || default_scheduled_at(task, date),
        title: override&.title.presence || task.title,
        description: override&.description.presence || task.description,
        canceled: override&.canceled || false,
        exception: override.present?,
        tags: task.tags.to_a
      )
    end

    def default_scheduled_at(task, date)
      return nil if task.due_time.blank?

      Time.utc(date.year, date.month, date.day, task.due_time.hour, task.due_time.min)
    end
  end
end
```

- [ ] **Step 5: Run, expect pass**

Run: `docker compose run --rm web bundle exec rspec spec/services/occurrences/calendar_spec.rb`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: occurrences calendar projection (virtual + exceptions)"
```

---

## Task 8: Serializers

Plain Ruby serializers (no gem) — explicit and dependency-free.

**Files:**
- Create: `app/serializers/task_serializer.rb`, `app/serializers/tag_serializer.rb`, `app/serializers/occurrence_serializer.rb`
- Test: covered by request specs (Tasks 9–11); no separate spec needed.

- [ ] **Step 1: TagSerializer**

`app/serializers/tag_serializer.rb`:
```ruby
class TagSerializer
  def initialize(tag) = @tag = tag

  def as_json(*)
    { id: @tag.id, name: @tag.name, system: @tag.system }
  end
end
```

- [ ] **Step 2: TaskSerializer**

`app/serializers/task_serializer.rb`:
```ruby
class TaskSerializer
  def initialize(task) = @task = task

  def as_json(*)
    {
      id: @task.id,
      title: @task.title,
      description: @task.description,
      status: @task.status,
      due_time: @task.due_time&.strftime("%H:%M"),
      recurrence: {
        type: @task.recurrence_type,
        interval: @task.recurrence_interval,
        days_of_month: @task.days_of_month,
        specific_dates: @task.specific_dates,
        parity: @task.parity,
        starts_on: @task.starts_on,
        ends_on: @task.ends_on
      },
      tags: @task.tags.map { |t| TagSerializer.new(t).as_json },
      created_at: @task.created_at,
      updated_at: @task.updated_at
    }
  end
end
```

- [ ] **Step 3: OccurrenceSerializer** (renders an `Occurrences::View`)

`app/serializers/occurrence_serializer.rb`:
```ruby
class OccurrenceSerializer
  def initialize(view) = @view = view

  def as_json(*)
    {
      task_id: @view.task.id,
      date: @view.date,
      status: @view.status,
      scheduled_at: @view.scheduled_at,
      title: @view.title,
      description: @view.description,
      canceled: @view.canceled,
      is_exception: @view.exception,
      tags: @view.tags.map { |t| TagSerializer.new(t).as_json }
    }
  end
end
```

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: plain-ruby serializers for tasks, tags, occurrences"
```

---

## Task 9: Routes + Tasks CRUD API (TDD request specs)

**Files:**
- Modify: `config/routes.rb`
- Create: `app/controllers/api/v1/tasks_controller.rb`
- Test: `spec/requests/api/v1/tasks_spec.rb`

- [ ] **Step 1: Define routes**

`config/routes.rb`:
```ruby
Rails.application.routes.draw do
  get "up" => "rails/health#show", as: :rails_health_check

  namespace :api do
    namespace :v1 do
      resources :tasks do
        resources :occurrences, only: %i[update destroy], param: :date,
                  constraints: { date: /\d{4}-\d{2}-\d{2}/ }
        resources :tags, only: %i[create destroy], controller: :task_tags
      end

      resources :occurrences, only: %i[index]
      resources :tags, only: %i[index create update destroy]
    end
  end
end
```

- [ ] **Step 2: Write failing request specs**

`spec/requests/api/v1/tasks_spec.rb`:
```ruby
require "rails_helper"

RSpec.describe "Api::V1::Tasks", type: :request do
  let(:json) { JSON.parse(response.body) }

  describe "POST /api/v1/tasks" do
    it "creates a one-off task" do
      post "/api/v1/tasks", params: {
        task: { title: "Подготовить отчёт", description: "Квартальный",
                recurrence_type: "once", starts_on: "2026-06-10", status: "new" }
      }
      expect(response).to have_http_status(:created)
      expect(json["title"]).to eq("Подготовить отчёт")
      expect(json["recurrence"]["type"]).to eq("once")
    end

    it "creates a recurring task with tags" do
      tag = create(:tag, name: "звонок")
      post "/api/v1/tasks", params: {
        task: { title: "Обзвон пациентов", recurrence_type: "daily",
                recurrence_interval: 1, starts_on: "2026-06-01", tag_ids: [tag.id] }
      }
      expect(response).to have_http_status(:created)
      expect(json["tags"].first["name"]).to eq("звонок")
    end

    it "returns 422 on invalid recurrence params" do
      post "/api/v1/tasks", params: {
        task: { title: "Bad", recurrence_type: "daily", recurrence_interval: 0,
                starts_on: "2026-06-01" }
      }
      expect(response).to have_http_status(:unprocessable_entity)
      expect(json["error"]).to be_present
    end
  end

  describe "GET /api/v1/tasks" do
    it "lists tasks filtered by status" do
      create(:task, status: "new", title: "A")
      create(:task, status: "done", title: "B")
      get "/api/v1/tasks", params: { status: "done" }
      expect(response).to have_http_status(:ok)
      expect(json["data"].map { |t| t["title"] }).to eq(["B"])
    end
  end

  describe "GET /api/v1/tasks/:id" do
    it "shows a task" do
      task = create(:task, title: "Обход")
      get "/api/v1/tasks/#{task.id}"
      expect(response).to have_http_status(:ok)
      expect(json["title"]).to eq("Обход")
    end

    it "returns 404 for a missing task" do
      get "/api/v1/tasks/0"
      expect(response).to have_http_status(:not_found)
    end
  end

  describe "PATCH /api/v1/tasks/:id" do
    it "updates a task" do
      task = create(:task, title: "Старое")
      patch "/api/v1/tasks/#{task.id}", params: { task: { title: "Новое" } }
      expect(response).to have_http_status(:ok)
      expect(json["title"]).to eq("Новое")
    end
  end

  describe "DELETE /api/v1/tasks/:id" do
    it "deletes a task" do
      task = create(:task)
      delete "/api/v1/tasks/#{task.id}"
      expect(response).to have_http_status(:no_content)
      expect(Task.exists?(task.id)).to be(false)
    end
  end
end
```

- [ ] **Step 3: Run, expect failure**

Run: `docker compose run --rm web bundle exec rspec spec/requests/api/v1/tasks_spec.rb`
Expected: FAIL.

- [ ] **Step 4: Implement the controller**

`app/controllers/api/v1/tasks_controller.rb`:
```ruby
module Api
  module V1
    class TasksController < BaseController
      def index
        scope = Task.with_status(params[:status]).order(:starts_on, :id)
        scope = scope.joins(:tags).where(tags: { name: params[:tag] }).distinct if params[:tag].present?
        pagy, tasks = pagy(scope)
        render json: {
          data: tasks.map { |t| TaskSerializer.new(t).as_json },
          meta: pagination_meta(pagy)
        }
      end

      def show
        render json: TaskSerializer.new(Task.find(params[:id])).as_json
      end

      def create
        task = Task.new(task_params)
        task.save!
        render json: TaskSerializer.new(task).as_json, status: :created
      end

      def update
        task = Task.find(params[:id])
        task.update!(task_params)
        render json: TaskSerializer.new(task).as_json
      end

      def destroy
        Task.find(params[:id]).destroy!
        head :no_content
      end

      private

      def task_params
        params.require(:task).permit(
          :title, :description, :status, :recurrence_type, :recurrence_interval,
          :parity, :starts_on, :ends_on, :due_time,
          days_of_month: [], specific_dates: [], tag_ids: []
        )
      end
    end
  end
end
```

- [ ] **Step 5: Run, expect pass**

Run: `docker compose run --rm web bundle exec rspec spec/requests/api/v1/tasks_spec.rb`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: tasks CRUD API with filtering and pagination"
```

---

## Task 10: Tags API + attach/detach (TDD request specs)

**Files:**
- Create: `app/controllers/api/v1/tags_controller.rb`, `app/controllers/api/v1/task_tags_controller.rb`
- Test: `spec/requests/api/v1/tags_spec.rb`

- [ ] **Step 1: Write failing request specs**

`spec/requests/api/v1/tags_spec.rb`:
```ruby
require "rails_helper"

RSpec.describe "Api::V1::Tags", type: :request do
  let(:json) { JSON.parse(response.body) }

  before { Tag::SYSTEM_NAMES.each { |n| Tag.find_or_create_by!(name: n) { |t| t.system = true } } }

  it "lists tags including system tags" do
    get "/api/v1/tags"
    expect(response).to have_http_status(:ok)
    expect(json.map { |t| t["name"] }).to include("операции", "звонок", "отчётность")
  end

  it "creates a custom tag" do
    post "/api/v1/tags", params: { tag: { name: "обход" } }
    expect(response).to have_http_status(:created)
    expect(json["system"]).to be(false)
  end

  it "forbids renaming a system tag" do
    tag = Tag.find_by(name: "операции")
    patch "/api/v1/tags/#{tag.id}", params: { tag: { name: "хирургия" } }
    expect(response).to have_http_status(:unprocessable_entity)
  end

  it "forbids deleting a system tag" do
    tag = Tag.find_by(name: "звонок")
    delete "/api/v1/tags/#{tag.id}"
    expect(response).to have_http_status(:unprocessable_entity)
    expect(Tag.exists?(tag.id)).to be(true)
  end

  describe "attach / detach" do
    it "attaches a tag to a task" do
      task = create(:task)
      tag  = create(:tag, name: "обход")
      post "/api/v1/tasks/#{task.id}/tags", params: { tag_id: tag.id }
      expect(response).to have_http_status(:created)
      expect(task.reload.tags).to include(tag)
    end

    it "detaches a tag from a task" do
      task = create(:task)
      tag  = create(:tag, name: "обход")
      task.tags << tag
      delete "/api/v1/tasks/#{task.id}/tags/#{tag.id}"
      expect(response).to have_http_status(:no_content)
      expect(task.reload.tags).not_to include(tag)
    end
  end
end
```

- [ ] **Step 2: Run, expect failure**

Run: `docker compose run --rm web bundle exec rspec spec/requests/api/v1/tags_spec.rb`
Expected: FAIL.

- [ ] **Step 3: Implement TagsController**

`app/controllers/api/v1/tags_controller.rb`:
```ruby
module Api
  module V1
    class TagsController < BaseController
      def index
        render json: Tag.order(:name).map { |t| TagSerializer.new(t).as_json }
      end

      def create
        tag = Tag.new(tag_params)
        tag.save!
        render json: TagSerializer.new(tag).as_json, status: :created
      end

      def update
        tag = Tag.find(params[:id])
        if tag.update(tag_params)
          render json: TagSerializer.new(tag).as_json
        else
          render json: { error: { message: "Ошибка валидации", details: tag.errors.full_messages } },
                 status: :unprocessable_entity
        end
      end

      def destroy
        tag = Tag.find(params[:id])
        if tag.destroy
          head :no_content
        else
          render json: { error: { message: "Ошибка валидации", details: tag.errors.full_messages } },
                 status: :unprocessable_entity
        end
      end

      private

      def tag_params
        params.require(:tag).permit(:name)
      end
    end
  end
end
```
Note: system protection lives in the model (`throw :abort` makes `update`/`destroy` return false), so the controller simply reports the model errors as 422.

- [ ] **Step 4: Implement TaskTagsController**

`app/controllers/api/v1/task_tags_controller.rb`:
```ruby
module Api
  module V1
    class TaskTagsController < BaseController
      def create
        task = Task.find(params[:task_id])
        tag  = Tag.find(params[:tag_id])
        task.task_tags.find_or_create_by!(tag: tag)
        render json: TaskSerializer.new(task.reload).as_json, status: :created
      end

      def destroy
        task = Task.find(params[:task_id])
        task.task_tags.where(tag_id: params[:id]).destroy_all
        head :no_content
      end
    end
  end
end
```

- [ ] **Step 5: Run, expect pass**

Run: `docker compose run --rm web bundle exec rspec spec/requests/api/v1/tags_spec.rb`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: tags API with system protection + attach/detach"
```

---

## Task 11: Occurrences API — calendar + single-instance mutations (TDD request specs)

The business-scenario heart: list a date window, complete one day independently, reschedule one day (exception), cancel one day.

**Files:**
- Create: `app/controllers/api/v1/occurrences_controller.rb`
- Test: `spec/requests/api/v1/occurrences_spec.rb`

- [ ] **Step 1: Write failing request specs**

`spec/requests/api/v1/occurrences_spec.rb`:
```ruby
require "rails_helper"

RSpec.describe "Api::V1::Occurrences", type: :request do
  let(:json) { JSON.parse(response.body) }

  describe "GET /api/v1/occurrences" do
    it "lists occurrences across a date window" do
      create(:task, :daily, recurrence_interval: 1, starts_on: Date.new(2026, 6, 1), title: "Обход")
      get "/api/v1/occurrences", params: { from: "2026-06-01", to: "2026-06-05" }
      expect(response).to have_http_status(:ok)
      expect(json["data"].size).to eq(5)
      expect(json["data"].first["title"]).to eq("Обход")
    end

    it "defaults to the current month when no window is given" do
      get "/api/v1/occurrences"
      expect(response).to have_http_status(:ok)
    end

    it "filters by status" do
      task = create(:task, :daily, recurrence_interval: 1, starts_on: Date.new(2026, 6, 1))
      task.occurrences.create!(occurrence_date: Date.new(2026, 6, 2), status: "done")
      get "/api/v1/occurrences", params: { from: "2026-06-01", to: "2026-06-05", status: "done" }
      expect(json["data"].map { |o| o["date"] }).to eq(["2026-06-02"])
    end
  end

  describe "PATCH /api/v1/tasks/:task_id/occurrences/:date" do
    it "marks only that day as done (independent state)" do
      task = create(:task, :daily, recurrence_interval: 1, starts_on: Date.new(2026, 6, 1))
      patch "/api/v1/tasks/#{task.id}/occurrences/2026-06-03", params: { occurrence: { status: "done" } }
      expect(response).to have_http_status(:ok)
      expect(json["status"]).to eq("done")
      expect(json["is_exception"]).to be(true)
      expect(task.occurrences.count).to eq(1) # only this day materialized
    end

    it "reschedules a single occurrence (exception)" do
      task = create(:task, :daily, recurrence_interval: 1, starts_on: Date.new(2026, 6, 1))
      patch "/api/v1/tasks/#{task.id}/occurrences/2026-06-03",
            params: { occurrence: { scheduled_at: "2026-06-03T14:00:00Z" } }
      expect(response).to have_http_status(:ok)
      expect(json["scheduled_at"]).to include("14:00")
    end

    it "returns 422 when the date is not part of the schedule" do
      task = create(:task, :monthly, days_of_month: [1], starts_on: Date.new(2026, 6, 1))
      patch "/api/v1/tasks/#{task.id}/occurrences/2026-06-02", params: { occurrence: { status: "done" } }
      expect(response).to have_http_status(:unprocessable_entity)
    end
  end

  describe "DELETE /api/v1/tasks/:task_id/occurrences/:date" do
    it "cancels only that day" do
      task = create(:task, :daily, recurrence_interval: 1, starts_on: Date.new(2026, 6, 1))
      delete "/api/v1/tasks/#{task.id}/occurrences/2026-06-03"
      expect(response).to have_http_status(:ok)
      expect(json["canceled"]).to be(true)
      expect(json["status"]).to eq("canceled")
    end
  end
end
```

- [ ] **Step 2: Run, expect failure**

Run: `docker compose run --rm web bundle exec rspec spec/requests/api/v1/occurrences_spec.rb`
Expected: FAIL.

- [ ] **Step 3: Implement the controller**

`app/controllers/api/v1/occurrences_controller.rb`:
```ruby
module Api
  module V1
    class OccurrencesController < BaseController
      def index
        from = parse_date(params[:from]) || Date.current.beginning_of_month
        to   = parse_date(params[:to]) || Date.current.end_of_month

        views = Occurrences::Calendar.new(
          scope: Task.all, from: from, to: to,
          status: params[:status], tag: params[:tag]
        ).occurrences

        pagy, page = pagy_array(views)
        render json: {
          data: page.map { |v| OccurrenceSerializer.new(v).as_json },
          meta: pagination_meta(pagy)
        }
      end

      def update
        task = Task.find(params[:task_id])
        date = Date.iso8601(params[:date])
        occ  = task.occurrences.find_or_initialize_by(occurrence_date: date)
        occ.assign_attributes(occurrence_params)
        occ.save!
        render json: OccurrenceSerializer.new(view_for(task, occ)).as_json
      rescue Date::Error
        render json: { error: { message: "Некорректная дата" } }, status: :bad_request
      end

      def destroy
        task = Task.find(params[:task_id])
        date = Date.iso8601(params[:date])
        occ  = task.occurrences.find_or_initialize_by(occurrence_date: date)
        occ.canceled = true
        occ.status   = "canceled"
        occ.save!
        render json: OccurrenceSerializer.new(view_for(task, occ)).as_json
      rescue Date::Error
        render json: { error: { message: "Некорректная дата" } }, status: :bad_request
      end

      private

      def occurrence_params
        params.require(:occurrence).permit(:status, :scheduled_at, :title, :description, :canceled)
      end

      def parse_date(value)
        value.present? ? Date.iso8601(value) : nil
      rescue Date::Error
        nil
      end

      # Build a single occurrence View from a (materialized) occurrence row,
      # reusing the same projection rules as the calendar.
      def view_for(task, occ)
        Occurrences::View.new(
          task: task,
          date: occ.occurrence_date,
          status: occ.status || task.status,
          scheduled_at: occ.scheduled_at || default_scheduled_at(task, occ.occurrence_date),
          title: occ.title.presence || task.title,
          description: occ.description.presence || task.description,
          canceled: occ.canceled,
          exception: true,
          tags: task.tags.to_a
        )
      end

      def default_scheduled_at(task, date)
        return nil if task.due_time.blank?

        Time.utc(date.year, date.month, date.day, task.due_time.hour, task.due_time.min)
      end
    end
  end
end
```

- [ ] **Step 4: Run, expect pass**

Run: `docker compose run --rm web bundle exec rspec spec/requests/api/v1/occurrences_spec.rb`
Expected: PASS.

- [ ] **Step 5: Run the full suite**

Run: `docker compose run --rm web bundle exec rspec`
Expected: all green.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: occurrences calendar API + per-instance mutations"
```

---

## Task 12: Swagger / OpenAPI via rswag

**Files:**
- Modify: `Gemfile`
- Create: `spec/swagger_helper.rb`, `spec/requests/api/v1/*_swagger_spec.rb` (rswag DSL), `swagger/v1/swagger.yaml`
- Modify: `config/initializers/rswag_*`, `config/routes.rb` (mount UI), `app/controllers/api/v1/base_controller.rb` (none)

- [ ] **Step 1: Add rswag gems & install**

In `Gemfile` (all envs):
```ruby
gem "rswag-api"
gem "rswag-ui"
```
In `:development, :test`:
```ruby
gem "rswag-specs"
```
Then:
```bash
docker compose build
docker compose run --rm web bin/rails g rswag:api:install
docker compose run --rm web bin/rails g rswag:ui:install
docker compose run --rm web bin/rails g rswag:specs:install
```

- [ ] **Step 2: Configure `spec/swagger_helper.rb`** (Russian info block, one v1 doc)

Set the `openapi_spec` info:
```ruby
config.openapi_specs = {
  "v1/swagger.yaml" => {
    openapi: "3.0.1",
    info: { title: "МИС — Трекер задач API", version: "v1",
            description: "API модуля трекера рабочих задач для персонала." },
    paths: {},
    servers: [{ url: "http://localhost:3000" }]
  }
}
```

- [ ] **Step 3: Author rswag request specs for the documented contract**

Create `spec/requests/api/v1/tasks_swagger_spec.rb` covering create/list/show/update/destroy. Example (full file follows this shape for each path):
```ruby
require "swagger_helper"

RSpec.describe "Tasks API", type: :request do
  path "/api/v1/tasks" do
    post "Создать задачу" do
      tags "Задачи"
      consumes "application/json"
      produces "application/json"
      parameter name: :task, in: :body, schema: {
        type: :object,
        properties: {
          task: {
            type: :object,
            properties: {
              title: { type: :string },
              description: { type: :string },
              status: { type: :string, enum: Task::STATUSES },
              recurrence_type: { type: :string, enum: Task::RECURRENCE_TYPES },
              recurrence_interval: { type: :integer },
              days_of_month: { type: :array, items: { type: :integer } },
              specific_dates: { type: :array, items: { type: :string, format: :date } },
              parity: { type: :string, enum: Task::PARITIES },
              starts_on: { type: :string, format: :date },
              ends_on: { type: :string, format: :date },
              due_time: { type: :string, example: "10:00" },
              tag_ids: { type: :array, items: { type: :integer } }
            },
            required: %w[title recurrence_type]
          }
        }
      }

      response "201", "задача создана" do
        let(:task) { { task: { title: "Обзвон", recurrence_type: "daily",
                               recurrence_interval: 1, starts_on: "2026-06-01" } } }
        run_test!
      end

      response "422", "ошибка валидации" do
        let(:task) { { task: { title: "", recurrence_type: "daily" } } }
        run_test!
      end
    end

    get "Список задач" do
      tags "Задачи"
      produces "application/json"
      parameter name: :status, in: :query, schema: { type: :string }, required: false
      parameter name: :tag, in: :query, schema: { type: :string }, required: false

      response "200", "ок" do
        run_test!
      end
    end
  end
end
```
Author analogous rswag specs for: `GET/PATCH/DELETE /api/v1/tasks/{id}`, `GET /api/v1/occurrences`, `PATCH/DELETE /api/v1/tasks/{task_id}/occurrences/{date}`, and the tags endpoints (`GET/POST/PATCH/DELETE /api/v1/tags`, attach/detach). Each documents the relevant params and the success + key error responses, mirroring the request specs from Tasks 9–11.

- [ ] **Step 4: Generate the swagger doc**

Run: `docker compose run --rm web bin/rails rswag:specs:swaggerize`
Expected: `swagger/v1/swagger.yaml` written; the rswag specs pass (they double as tests).

- [ ] **Step 5: Serve the UI at `/api-docs` and verify**

Confirm `config/initializers/rswag_ui.rb` references `v1/swagger.yaml`. Then:
```bash
docker compose up -d
curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:3000/api-docs/index.html
docker compose down
```
Expected: `200`.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "docs: swagger/openapi via rswag served at /api-docs"
```

---

## Task 13: README (Russian), final polish & HISTORY placeholder

**Files:**
- Create: `README.md` (Russian), `HISTORY.md` (placeholder note)
- Verify: full stack boots from a clean checkout.

- [ ] **Step 1: Write `README.md` in Russian** with these sections:

1. **Описание** — что это за модуль (трекер задач МИС).
2. **Стек** — Ruby 3.4, Rails 8 API, PostgreSQL 16, Docker.
3. **Быстрый запуск** — точные команды:
   ```bash
   docker compose build
   docker compose up
   # API:      http://localhost:3000
   # Swagger:  http://localhost:3000/api-docs
   ```
   Плюс как прогнать тесты: `docker compose run --rm web bundle exec rspec`.
4. **Архитектура повторяемости** — серия (`Task`) + виртуальные вхождения (`Recurrence::Generator`) + материализация исключений (`TaskOccurrence`); как решены «проблема бесконечности», независимый статус экземпляра и исключения.
5. **Обзор API** — таблица эндпоинтов (`/api/v1/tasks`, `/api/v1/occurrences`, `/api/v1/tags`, attach/detach, мутации одного вхождения) + 2–3 примера `curl`.
6. **Принятые допущения** (обязательный раздел):
   - Аутентификация и пользователи не реализованы — строго в рамках ТЗ.
   - Даты — календарные в одной таймзоне (по умолчанию UTC); время дня хранится в `due_time`.
   - Статусы: `new`, `in_progress`, `done`, `canceled` (свободные переходы).
   - Вхождение адресуется парой `(task_id, date)`; `date` в формате `YYYY-MM-DD`.
   - Перенос (`scheduled_at`) сохраняет якорную дату слота `occurrence_date`.
   - Бессрочные серии требуют ограниченного окна; окно ограничено `MAX_WINDOW_DAYS = 366`, дефолт — текущий месяц.
   - Системные теги (`отчётность`, `операции`, `звонок`) создаются сидами и защищены от изменения/удаления на уровне модели.
7. **Структура проекта** — краткая карта `app/services` (движок и проекция), моделей, контроллеров.

- [ ] **Step 2: Add `HISTORY.md` placeholder**

```markdown
# История работы с LLM

Чистая и структурированная выгрузка диалога с LLM будет собрана отдельно
перед финальной сдачей.
```
(Curated separately, per agreement with the stakeholder.)

- [ ] **Step 3: Clean-boot verification from scratch**

```bash
docker compose down -v
docker compose build
docker compose up -d
sleep 5
curl -sS http://localhost:3000/api/v1/tags          # 3 system tags present
curl -sS "http://localhost:3000/api/v1/occurrences?from=2026-06-01&to=2026-06-07"
docker compose run --rm web bundle exec rspec        # full suite green
docker compose down
```
Expected: system tags returned, occurrences endpoint responds, all specs pass.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "docs: Russian README, assumptions, HISTORY placeholder"
```

---

## Self-Review (coverage check)

| Spec requirement | Covered by |
|---|---|
| CRUD задач (create/edit/delete/show/list) | Task 9 |
| Фильтрация списка по дате и статусам | Task 9 (status on `/tasks`) + Task 11 (date window + status on `/occurrences`) |
| Задача имеет название, описание, дату, статус | Task 3 (model) |
| Теги: добавлять/удалять, many-to-many | Task 4 + Task 10 (attach/detach) |
| Обязательные системные теги, запрет изменения/удаления | Task 2 (model + seeds) + Task 10 (422 specs) |
| Периодичность: ежедневно (каждый n-й) | Task 6 (`daily`) |
| Периодичность: ежемесячно (число 1–31) | Task 6 (`monthly`, Feb-31 skip) |
| Периодичность: конкретные даты | Task 6 (`specific_dates`) |
| Периодичность: чётные/нечётные дни | Task 6 (`parity`) |
| Проблема бесконечности | Task 6 (window clamp) + Task 7/11 (bounded calendar) |
| Независимое состояние экземпляра | Task 7 + Task 11 (per-day materialization) |
| Исключения из правил (бонус: перенос/отмена одного дня) | Task 11 (reschedule via `scheduled_at`, cancel via DELETE) |
| Ruby 3.4 / PostgreSQL 16 | Task 0 (Docker images) |
| Docker / docker-compose | Task 0 |
| Инструкция запуска | Task 13 (README) |
| Swagger документация | Task 12 |
| HISTORY.md | Task 13 (placeholder; curated later) |

No placeholders remain; type/method names are consistent across tasks (`Recurrence::Generator#dates_between`, `Occurrences::Calendar#occurrences`, `Occurrences::View`, `TaskSerializer`/`TagSerializer`/`OccurrenceSerializer`).

---

## Parallelization notes (for subagent-driven execution)

Tasks 0 → 5 are sequential (schema foundations). After Task 5, two tracks can run in parallel:
- **Track A:** Task 6 (Recurrence::Generator) → Task 7 (Calendar projection).
- **Track B:** Task 8 (serializers) → Task 9 (Tasks CRUD) → Task 10 (Tags API).

Task 11 depends on both tracks (needs the generator/projection AND routes/controllers). Tasks 12–13 are last and sequential. Keep each task's commit atomic so review between tasks stays clean.
